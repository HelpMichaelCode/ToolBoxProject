using System;
using System.Collections.Generic;

namespace Toolbox
{

public class Test
{
    //static void GetDatesBetween(DateTime startDate, DateTime endDate)
    //{
    //    // List of dates
    //    List<DateTime> allDates = new List<DateTime>();
    //    Random rand = new Random();
    //    for (DateTime date = startDate; date <= endDate; date = date.AddDays(1))
    //    {
    //        allDates.Add(date);

    //    }
    //    allDates.ToArray();

    //    for (int i = 0; i < allDates.ToArray().Length; i++)
    //    {
    //        Console.WriteLine(allDates[i].Date.AddSeconds(rand.Next(60 * 60 * 24)).ToString());
    //    }
    //}

    //    public static double GetRandomNumber(double minimum, double maximum)
    //    {
    //        Random random = new Random();
    //        return minimum + (random.NextDouble() * (maximum - minimum));
    //    }

    //    static void Main(string[] args)
    //    {
    //        Random rand = new Random();
    //        //double number;
    //        //number = rand.NextDouble() * 10;
    //        //Console.WriteLine($"{number}");

    //        //string[] test = { "Automation", "Direct Reveal EU", "Direct Reveal US" };

    //        //Guid uuid = Guid.NewGuid();
    //        //Console.WriteLine(uuid.ToString());


    //        //Console.WriteLine($"{test[rand.Next(0, 3)]}");

    //        //string startDate = "01/03/2020";
    //        //DateTime now = DateTime.Now;

    //        //DateTime startD = DateTime.Parse(startDate);

    //        //GetDatesBetween(startD, now);

    //        int number;

    //        for (int i = 0; i < 10; i++)
    //        {
    //            number = rand.Next(10) +  1;
               
    //            Console.WriteLine(number);
    //        }

    //    }
    }
}
